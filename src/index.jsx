import React from "react";
import ReactDOM from "react-dom/client";
import { GifExpressApp } from "./GifExpressApp";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GifExpressApp />
  </React.StrictMode>
);
